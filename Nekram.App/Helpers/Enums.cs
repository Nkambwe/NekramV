namespace Nekram.App.Helpers {

    public enum StateKeys {
        Error,
        Counter,
        LastRequest,
        Url
    }

    public enum SessionKeys {
        User,
        Email,
        LogoImage,
        Company
    }
}